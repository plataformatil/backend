spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/plataformatil
    username: postgres
    password: tec123
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
